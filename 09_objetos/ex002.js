const dadosAlfredo = {
    nome: "Alfredo",
    idade: 27,
    endereco: "Rua que sobe e desce",
    numeroIdentificador: 2135432
};

console.log(`Esses são os dados do cidadão: nome -> ${dadosAlfredo.nome}, idade -> ${dadosAlfredo.idade}, endereço -> ${dadosAlfredo.endereco}, NI -> ${dadosAlfredo.numeroIdentificador}`);